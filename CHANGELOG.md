# 변경이력

각 LOOP의 검증을 통과한 실제 제품 변화를 즉시 기록합니다. 실패한 시도와 예정 작업은 기록하지 않으며, 실습 마지막에는 누락과 버전 표기만 정리합니다.

## [Unreleased]

### LOOP-01 — 재고 조회와 기본 출고

- WBS 정확 일치 필터와 재고 행 선택을 연결했습니다.
- 출고 입력을 정규화하고 `IM24-001` 재고 12개 출고 시 `100 → 88`, 상태 `COMPLETED`, 일반 프로젝트 출고 이동유형 `221`을 기록합니다.
- 검증: Domain·Service·API·Frontend 대상 테스트 통과, 브라우저에서 필터 1건 조회와 `100 → 88` 확인.

### LOOP-02 — 안전한 출고

- 0·음수 수량, 재고 부족, 없는 재고 조합, 중복 요청 번호를 저장소 변경 전에 차단합니다.
- 실패 시 재고·출고 이력·요청 번호 등록이 모두 불변인 원자적 오류 처리를 적용했습니다.
- 검증: 재고 `8`에서 `9` 출고 시 오류와 재고 `8` 유지, `GI-001` 중복 요청 시 재고 `88`·이력 1건 유지 확인.

### LOOP-03 — 전체 취소와 이동유형

- 완료 건만 전체 취소할 수 있고 원 출고 수량 전체를 원복하며 재취소를 차단합니다.
- 일반 프로젝트 `221/222`, 가수주 `M75/M76`, 하자보수 `M77/M78`의 출고·취소 코드 쌍을 적용했습니다.
- 검증: 브라우저에서 `88 → 100`, `COMPLETED → CANCELED`, `221 → 222` 확인 및 Domain·Service 회귀 테스트 통과.

### LOOP-04 — 화면 통합과 회귀

- 정상·오류·로딩·빈 목록 상태, 완료/취소 배지, 요청 중 버튼 잠금, 데모 초기화와 반응형 화면을 완성했습니다.
- HTTP 400의 문자열 `detail`과 422의 배열 `detail`을 모두 사용자 메시지로 처리합니다.
- 검증: 전체 자동 테스트 `23/23`, 480px 단일 열 레이아웃, 초기화 후 재고 `100/20/8`·빈 이력·기본 폼 복원, 브라우저 콘솔 오류 0건 확인.
